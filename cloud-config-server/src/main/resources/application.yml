
server:
  port: 8888
spring:
  cloud:
    config:
      server:
        git:
          default-label: multiple-instances
          uri: https://github.com/ndourcodeur/config-server
  application:
    name: config-server
