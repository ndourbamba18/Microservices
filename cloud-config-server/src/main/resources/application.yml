
server:
  port: 8888
spring:
  cloud:
    config:
      server:
        git:
          default-label: eureka
          uri: https://github.com/ndourcodeur/config-server
  application:
    name: config-server
